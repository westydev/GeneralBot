<%- include('includes/header'); -%>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
    </head>
    <body>
        <section>
            <div style="display: flex; flex-wrap: wrap; justify-content: center" class="md:pl-2">
            <% if(user && user.id){ %>
                   <% settingList.forEach(setting => { %>
                   <!--<div class="card text-white bg-dark mb-3">
                            <h5 class="card-title text-primary" style="font-size: 25px;"><%= setting.name %></h5>
                            <p class="card-text" style="font-size: 12px;font-style:italic;color: #b3b3b3"><b><%= setting.despriction %></b></p>
                              <a style="font-size: 25px" href=" http://localhost:3000/dashboard/<%- guild.id %>/<%- setting.id %>"
                                        class="mt-auto btn btn-lg btn-block btn-success"><%= setting.action %> .</a>
                   </div>-->
<div class="justify-center bg-gray-900 text-white w-64 h-39 rounded-xl  mb-6 ml-2">

    <h5 class="text-xl font-bold text-white pl-2 pt-2 "><%= setting.name %></h5>
<p class="text-gray-400 text-lg pb-4 pl-2"><%= setting.despriction%></p>
    <a data-theme="night" class="btn w-full h-max" href="/dashboard/<%- guild.id %>/<%- setting.id %>/"><%- setting.action%></a>
              
</div>
                   <% }) %>
            <% }else {  %>
                    <p>Please login First!</p>
            <%  } %>
            </div>
        </section>
        <%- include('includes/footer'); -%>
    </body>
</html>
